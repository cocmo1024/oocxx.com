---
interface Props {
	title: string;
	eyebrow?: string;
	compact?: boolean;
}

const { title, eyebrow, compact = false } = Astro.props;
---

<section class:list={["sidebar-card", { compact }]}>
	{eyebrow && <p class="eyebrow">{eyebrow}</p>}
	<h3>{title}</h3>
	<slot />
</section>
